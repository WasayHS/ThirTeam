package loot;

import javafx.scene.layout.GridPane;
import javafx.scene.shape.Rectangle;
import map.Position;
import unit.Unit;

public class StrPotion extends Collectible{
	// No clue what stats we want, Pots are separated in child classes.
	public StrPotion() {
		super(/* add, stats, here*/);
	}
}
